{
  "name": "media.texasmovement.com",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "postbuild": "node scripts/check-public-output.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "typecheck": "astro check",
    "test": "vitest run",
    "check:constants": "node packages/constants/scripts/check.mjs --strict",
    "check:output": "node scripts/check-public-output.mjs",
    "ci": "npm run check:constants && npm run build && npm run typecheck && npm run test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "@tmi/constants": "file:./packages/constants",
    "astro": "^5.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@axe-core/playwright": "^4.13.0",
    "playwright": "1.56.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
